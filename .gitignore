# ─── Build outputs ────────────────────────────────────────────────────────────
/bin/
*.exe

# ─── Runtime data ─────────────────────────────────────────────────────────────
*.db
*.db-shm
*.db-wal

# ─── Personal config (contains real secrets — never commit) ─────────────────
# Copy openclaw.example.yaml → openclaw.yaml and fill in your real values.
# The binary reads openclaw.yaml by default; it stays local only.
openclaw.yaml

# ─── OS & Editor ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
